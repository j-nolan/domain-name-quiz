import React from 'react';

const Header = () => (
  <div>
    <h1 className="app-title">domain name quiz</h1>
    <p className="tag-line">How well do you know domain names?</p>
  </div>
);

export default Header;
